# --- Switch for backend Influx connectivity ---
INFLUX_MODE=remote   # remote | local

# --- InfluxDB (common) ---
INFLUXDB_ORG=your-org
INFLUXDB_BUCKET=your-bucket
INFLUXDB_TOKEN=your-token

# --- Remote (SSH tunnel) ---
SSH_USER=username
SSH_HOST=your-remote-server.com
SSH_PORT=22
SSH_KEY_PATH=~/.ssh/id_rsa         # host path (NOT in repo)
REMOTE_INFLUX_HOST=localhost
REMOTE_INFLUX_PORT=8086
LOCAL_FORWARD_PORT=8086            # port exposed inside the docker network

# --- Local EXISTING InfluxDB (no container) ---
LOCAL_INFLUXDB_SCHEME=http
LOCAL_INFLUXDB_HOST=host.docker.internal   # on Linux you may need 172.17.0.1
LOCAL_INFLUXDB_PORT=8086

# --- Service ports ---
BACKEND_PORT=3000
FRONTEND_PORT=5173